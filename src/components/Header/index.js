import React from 'react';

import { Scrollbars } from 'react-custom-scrollbars';

import './Header.scss';

const Header = props => {
    return (
      <header>
        <h1>Pixel</h1>
      </header>
    );
};

export default Header;
